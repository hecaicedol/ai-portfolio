# Minimal dev requirements for P3 Slice 1 — no Postgres, no real LLM, no money.
# Heavy production-only deps (tavily-python, arxiv, reportlab, voyageai, langfuse)
# are deferred to Slice 2/3 when real services are wired.

pydantic==2.9.2
pydantic-settings==2.6.0
numpy>=1.26,<3
tiktoken==0.8.0

# Agent orchestration
langchain-core==0.3.15
langgraph==0.2.45

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
